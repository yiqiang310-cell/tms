Day 1 总结：从 Prompt Script 到 Agent 思维
一、今天做了什么

你最开始的代码，本质是：

LLM判断 → 返回特殊字符串 → Python解析 → 调工具 → 再次调用LLM

比如：

用户问题
↓
LLM返回：0,爸
↓
Python解析：
answer[0] == "0"
↓
调用数据库
↓
把结果喂回LLM
↓
最终回答

代码核心：

if answer[0] == "0":

后来你升级成：

Structured Output（结构化输出）

从：

0,爸

升级成：

{
    "type":"tool",
    "relation":"爸"
}

代码：

answer = json.loads(answer)

这一步非常关键。

二、今天发现的第一个问题
问题：字符串解析太脆弱

你原来的：

answer[0]
answer[2]

问题：

模型只要输出：

0, 爸

或者：

0，爸

代码就炸。

今天学到：

Agent不能依赖字符串hack。

应该：

让模型输出结构化数据。

也就是：

json.loads(answer)
今天完成：

✅ 从字符串解析升级到 JSON parsing

三、今天发现的第二个问题
问题：模型 hallucination（幻觉）

你输入：

我姐的关系

模型返回：

{
    "type":"tool",
    "relation":"女弟"
}

然后：

KeyError: '女弟'

程序直接崩。

今天学到：

这是 Agent开发最核心的一句话：

LLM输出永远不可信。

Prompt可以降低错误。

但：

Prompt不能保证正确。

四、今天学到 Agent 的三层防线（非常重要）
第一层：Prompt约束

模型要知道：

relation只能是：

["弟", "姐", "哥"]

不能自由发挥。

比如 Prompt 要写：

relation字段只能从：

["弟","姐","哥"]

中选择。

作用：

降低 hallucination。

第二层：Output验证

即使 Prompt写好了。

模型仍可能输出：

{
    "relation":"女弟"
}

所以：

代码必须验证。

比如：

if relation not in VALID_RELATIONS:

作用：

拒绝非法参数。

第三层：Tool防御

即使模型错了。

工具也不能崩。

你原来：

return data_weather[identifys]

会：

KeyError

改成：

return data_weather.get(
    identifys,
    "NOT_FOUND"
)

作用：

防止系统崩溃。

今天最重要的一句话

你今天真正学到的是：

LLM负责建议，代码负责裁决。

不是：

模型说什么，我就执行什么

而是：

模型建议
↓
代码验证
↓
合法才执行

这就是：

AI工程思维。

五、你现在处于哪个阶段？

如果按 Agent学习路线：

Level 0：API调用

只会：

chat("你好")
Level 1：Prompt控制

你已经会：

Few-shot：

messages.append(...)
Level 2：Tool Calling（你现在）

你已经会：

LLM Decision
↓
Tool
↓
Observation
Level 3：ReAct（下一步）

你还差：

Thought
↓
Action
↓
Observation
↓
Loop
↓
Final
你当前：

Level 2（Tool Calling）

不是零基础了。

六、明天任务（Day 2）

目标：

把 Tool Calling 升级成真正的 ReAct Loop。

你要做：

任务1：补三层防线

完成：

✅ Prompt枚举约束
✅ Output验证
✅ Tool防御

任务2：加 while循环

从：

if answer["type"] == "tool":

升级成：

while True:

直到：

{
    "type":"final"
}

停止。

流程变成：

用户
↓
Thought
↓
Action
↓
Observation
↓
Thought
↓
Final
